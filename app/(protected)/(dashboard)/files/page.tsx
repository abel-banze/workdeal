import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Workdeal | Ficheiros",
    description: "Work and make deal.",
};

export default function Files(){
    return (
        <>
            files
        </>
    )
}
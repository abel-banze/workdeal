{
    "version": 2,
    "builds": [
      {
        "src": "/",
        "use": "@vercel/next",
        "config": {
          "buildCommand": "next build && npx prisma generate"
        }
      }
    ]
  }